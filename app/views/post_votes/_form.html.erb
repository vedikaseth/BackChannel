<%= form_for(@post_vote) do |f| %>
  <% if @post_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_vote.errors.count, "error") %> prohibited this post_vote from being saved:</h2>

      <ul>
      <% @post_vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= f.label :post_id %><br />
      <%= f.text_area :post_id, :cols => 50, :rows =>5 %>
    </div>
    <div class="field">
      <%= f.label :user_id %><br>
      <%= f.text_area :user_id, :cols => 50, :rows =>5 %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
